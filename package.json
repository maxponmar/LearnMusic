{
  "name": "learn-acoustic-guitar",
  "version": "0.1.0",
  "private": true,
  "description": "A worship-guitar learning app: play by ear, improvise, serve the song.",
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "dev": "pnpm -r --parallel --stream run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "db:migrate": "pnpm --filter @lag/api db:migrate",
    "clean": "pnpm -r exec rm -rf dist build .astro node_modules .pnpm-debug.log* && rm -rf node_modules"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
